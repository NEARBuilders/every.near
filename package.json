{
  "name": "every.near",
  "version": "1.0.0",
  "scripts": {
    "dev": "bos-workspace dev",
    "build": "bos-workspace build"
  },
  "devDependencies": {
    "bos-workspace": "^0.0.1-alpha.4"
  }
}
